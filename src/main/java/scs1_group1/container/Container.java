package scs1_group1.container;

/**
 * Marker interface to serve as a common type for all container classes.
 * Currently, it does not declare any methods but provides a common parent type.
 */
public interface Container {
    // No methods; serves as a common type for containers.
}
